#include <stdbool.h> //Booleans
#include <stdlib.h>
#include <zconf.h> //Sleep
#include <time.h>

#include "Bears.h"





void bear(){
    struct Bear bear;

    srand(time(NULL)); //Fastest possible call after 10 seconds, so no problem with seed

    while (true){

        sleep(BEAR_SLEEP);

//        int hunger_rand = rand() %
]

    }

}
